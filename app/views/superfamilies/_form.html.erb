<%= form_with(model: superfamily, local: true) do |form| %>
  <% if superfamily.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(superfamily.errors.count, "error") %> prohibited this superfamily from being saved:</h2>

      <ul>
      <% superfamily.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
