<%= form_with(model: genus, local: true) do |form| %>
  <% if genus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genus.errors.count, "error") %> prohibited this genus from being saved:</h2>

      <ul>
      <% genus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :family_id %>
    <%= form.collection_select :family_id, Family.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
