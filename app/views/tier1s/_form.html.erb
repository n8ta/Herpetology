<%= form_with(model: tier1, local: true) do |form| %>
  <% if tier1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tier1.errors.count, "error") %> prohibited this tier1 from being saved:</h2>

      <ul>
      <% tier1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
