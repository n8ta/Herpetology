<%= form_with(model: tier3, local: true) do |form| %>
  <% if tier3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tier3.errors.count, "error") %> prohibited this tier3 from being saved:</h2>

      <ul>
      <% tier3.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :tier2_id %>
    <%= form.text_field :tier2_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
