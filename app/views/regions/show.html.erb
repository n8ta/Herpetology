<h2><%= link_to @taxon.common_names[0].name, taxon_path(@taxon) %> > <%= @taxon.name.titleize %></h2>

<%= react_component('Tierlist', {taxon_id: @taxon.id, id: @region.id}) %>


<p>Currently we have <%= @species.length %> species in this region with photos
  and <%= @tier.taxons.species.select {|sp| sp.root == @taxon}.length %> total</p>
<hr>
<%= react_component('SpeciesPicker', {image_path: @photo.image_path.url,
                                      options: @options}) %>